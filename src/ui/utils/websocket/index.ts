/**
 * WebSocket Utilities
 * 
 * Exports for WebSocket-related utility classes that manage connections
 * outside of React's render cycle.
 */

// Export ConnectionManager
export { 
  SocketConnectionManager,
  getSocketConnectionManager 
} from './SocketConnectionManager';